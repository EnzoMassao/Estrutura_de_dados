class No:
    def __init(self,id,aluno):
        self.id=id
        self.aluno =aluno
        self.proximo =None
        self.anterior= None
def menu():
    print("1- inserir no")
    print("2- listar no´s")
    print("3- remover no")
    print("4- veririficar no")
    opcao = int(input("Digite qual opcao deseja:"))
    return opcao

def inserir_no(No,identificador,aluno):
    novo_no= No(identificador,aluno)
    if lista is None:
        lista =novo_no
        return lista
    novo_no.proximo =lista
    lista.anterior =novo_no
    lista =novo_no
    return lista

def listar_no(lista):
    if aux.anterior == None:
        print("Anterior: None")
    else:
        print("anterior:", aux.anterior.aluno)
        print("aluno:", aux.aluno)
    if aux.proximo== None:
        print("proximo :None")
    else:
        print("proximo:", aux.proximo.aluno)
    aux= aux.proxim


def verificar(lista,id,aluno):
    aux =lista
    if lista is None:
        print("lista vazia.")
        return lista
    while aux != None:
        if aux.id == id:
            print("aluno:", aux.aluno)
            return lista
        aux.proximo

def remover_no(lista,identificador):
    aux= lista
    if lista is None:
        print("lista vazia")
        return lista
    while lista != None:
        if aux.id == id:
            if aux.proximo == aux.anterior == None:
                lista=None
                return lista
            elif aux == lista:
                lista =lista.proximo
                lista.anterior =None
                return lista
            elif aux.proximo == None:
                aux.anterior.proximo =None
                return lista
            aux.proximo.anterior= aux.anterior
            aux.anterior.proximo= aux.proximo
            return list
        aux= aux.proximo



def main():
    lista =None
    opcao = 0


    while opcao != 5:
        opcao = menu()

        if opcao ==1:
            id= int(input("digite o id:"))
            nome = input("digite o nome:")
            
            lista= inserir_no(lista,id, nome)



        elif opcao ==2:
            listar_no(lista)
        elif opcao ==3:
            identificador= int(input("digite o nome de quem vc quer remover:"))
            lista= remover_no(lista,identificador):

        elif opcao ==4:
            id= int(input("digite o nome que deseja:"))
            verificar(lista,id)


        elif opcao ==5:
            print("encerrando")


        else:
            print("opcao invalida.")
        
        



main()
