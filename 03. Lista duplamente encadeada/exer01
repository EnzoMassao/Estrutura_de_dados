class No:
    def __init__(self,identificador,nome):
        self.identificador=identificador
        self.nome =nome
        self.proximo =None
        self.anterior= None
def menu():
    print("1- inserir no")
    print("2- listar no´s")
    print("3- remover no")
    print("4- veririficar no")
    opcao = int(input("Digite qual opcao deseja:"))
    return opcao

def inserir_no(lista,identificador,nome):
    novo_no= No(identificador,nome)
    if lista is None:
        lista =novo_no
        return lista
    
    novo_no.proximo =lista
    lista.anterior =novo_no
    lista =novo_no
    return lista

def listar_no(lista):
    aux = lista
    if lista is None:
        print("lista vazia")
    while aux != None:
        if aux.anterior == None:
            print("Anterior: None")
        else:
            print("anterior:", aux.anterior.nome)
        print("nome:", aux.nome)
        if aux.proximo == None:
            print("proximo :None")
        else:
            print("proximo:", aux.proximo.nome)
        aux = aux.proximo

def verificar(lista,identificador):
    aux =lista
    if lista is None:
        print("lista vazia.")
        return lista
    while aux != None:
        if aux.identificador == identificador:
            print("nome:", aux.nome)
            return lista
        aux = aux.proximo
        

def remover_no(lista,identificador):
    aux= lista
    if lista is None:
        print("lista vazia")
        return lista
    while aux != None:
        if aux.identificador == identificador:
            if aux.proximo == aux.anterior == None:
                lista=None
                return lista
            elif aux == lista:
                lista =lista.proximo
                lista.anterior =None
                return lista
            elif aux.proximo == None:
                aux.anterior.proximo =None
                return lista
            aux.proximo.anterior= aux.anterior
            aux.anterior.proximo= aux.proximo
            return lista
        aux= aux.proximo



def main():
    lista =None
    opcao = 0


    while opcao != 5:
        opcao = menu()

        if opcao ==1:
            identificador= int(input("digite o identificador:"))
            nome = input("digite o nome:")
            
            lista= inserir_no(lista,identificador, nome)



        elif opcao ==2:
            listar_no(lista)
        elif opcao ==3:
            identificador= int(input("digite o nome de quem vc quer remover:"))
            lista= remover_no(lista,identificador)

        elif opcao ==4:
            identificador= int(input("digite o nome que deseja:"))
            verificar(lista,identificador)


        elif opcao ==5:
            print("encerrando")


        else:
            print("opcao invalida.")
        
        



main()
